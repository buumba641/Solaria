# Voice Layer (CLI)

This folder simulates ElevenLabs-style voice synthesis with a CLI so it can be
validated before frontend integration.

## Usage

```bash
cd backend/voice
python3 voice_cli.py --text "Hello from Solaria"
```

## Feature flag

```bash
export ELEVENLABS_ENABLED=true
```
